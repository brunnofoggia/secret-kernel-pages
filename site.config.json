{
  "url": "",
  "_url": "The full origin the site is served from, no trailing slash — e.g. https://secret-kernel.example.com. Drives dist/CNAME, the canonical link, og:url, robots.txt and sitemap.xml. Leave empty to build for local preview only; scripts/build.mjs then skips all of those and says so, and the deploy workflow refuses to run."
}
